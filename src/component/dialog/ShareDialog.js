import React from 'react'

const ShareDialog = ({ shareInfo }) => {
    const { url, type, title, description, image } = shareInfo

    return (
        <>
            <h3>Share</h3>
        </>
    )
}

export default ShareDialog